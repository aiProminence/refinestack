# RefineStack Release 1 verification log

Updated: 2026-08-16. A defect is closed only after its original failure and a regression neighborhood pass. Hosted SQL probes run inside `BEGIN … ROLLBACK` unless the row says otherwise.

| ID | Severity | Acceptance | Defect | Resolution | Retest evidence | State |
|---|---|---|---|---|---|---|
| D-001 | P0 | AT-002, AT-002A | A forwarded Supabase invite/recovery link could establish an Auth session and, for existing users, reset the password before mailbox ownership was proved. | Initial notice is now a non-authenticating claim sent through Resend; a separate mailbox OTP is required. Acceptance is service-only and membership sessions are bounded by `session_not_before`. | Source/auth tests plus hosted stale-session and acceptance rollback probes. | Closed |
| D-002 | P0 | AT-002 | Authenticated invitation rows allowed direct `accepted_at` updates, bypassing the app freshness check; bootstrap onboarding had the same route-gating weakness. | Revoked client invitation updates; added service-only acceptance/bootstrap RPCs requiring a freshly verified timestamp. | Hosted client-grant, pre/post-verification JWT and bootstrap assertions. | Closed |
| D-003 | P1 | AT-002, AT-010, AT-071 | A reusable claim could repeatedly trigger OTP sends; failed initial email delivery left an open invitation. | Atomic OTP admission/finalization, a single 15-minute active window, failure cooldown, claim consumption, explicit delivery states/events, and automatic revocation after failed initial delivery. | 5 invitation tests and hosted claim-consumption/replay rollback probe. | Closed |
| D-004 | P1 | AT-006, AT-007 | Concurrent owner changes could remove the last owner; ordinary cascade also made intended owner workspace deletion impossible. | Workspace-row serialization preserves the last-owner invariant; deletion moved to a service-only, owner-confirmed, freshly reauthenticated RPC with a durable receipt. | Hosted admin/foreign/stale/inexact/owner-success deletion suite. | Closed |
| D-005 | P1 | AT-003, AT-019–025 | Tenant IDs were not enforced across related records, and client roles could bypass atomic question-set/evidence lineage writes. | Composite workspace/project foreign keys, RLS on every public table, direct snapshot grants revoked, service-only atomic mutations. | Hosted schema assertions and cross-tenant/direct-insert rollback probes. | Closed |
| D-006 | P1 | AT-028 | First-use idempotency requests could race across instances; API replay also compared a caller-controlled estimate with the canonical stored estimate. | Advisory transaction lock on `(workspace,key)`, canonical request fingerprint, and replay comparison limited to immutable question/provider cohort. | Hosted same-key replay assertion and API schema regression. | Closed |
| D-007 | P1 | AT-027, AT-035 | Preflight counted one call per capture, trusted caller cost, and did not reserve against workspace cost quota. | Server-owned provider worst-case call/cost caps, atomic workspace-wide actual-plus-reserved admission, exact shortfalls, and UI disclosure. | Hosted zero-cost/call admission probes; repository budget tests. | Closed |
| D-008 | P1 | AT-034 | There was no authoritative cancellation workflow. | Service-only replay-safe cancel RPC, API route, UI control, reason/actor persistence, queued-job cancellation and cancelled-run preservation. | 26 cancellation/export tests and hosted first/replay cancellation assertions. | Closed |
| D-009 | P1 | AT-036, AT-037 | Scheduled skip-overlap and failure circuit behavior were absent or ineffective. | Atomic due-schedule overlap policy, consecutive-failure tracking and circuit opening. | Hosted overlap and three-terminal-failure rollback probes. | Closed |
| D-010 | P1 | AT-013, AT-016, AT-038 | Locale and disqualification-reason-only edits did not always create an immutable question version; clients could rewrite trigger-owned columns. | Recreated version triggers/hashes with locale and reason; narrowed question update grants. | Hosted locale/reason version assertions. | Closed |
| D-011 | P1 | AT-014, AT-015, AT-018 | Question metadata, near-duplicate prevention and coverage diagnostics existed only as disconnected helpers. | Deterministic create/edit/restore validation, similarity diagnostics, coverage matrix, DB active-question constraints and run-item execution guard. | 12 question-quality tests plus hosted incomplete/normalized-duplicate rejection. | Closed |
| D-012 | P1 | AT-029–033, AT-042, AT-047 | Classifier/runtime mishandled empty, Unicode and adversarial recommendation cases; provider retries and usage had low path coverage. | Structured evidence spans, deterministic fact implications, bounded provider calls, exact-once worker persistence and expanded adversarial coverage. | 87+ intelligence/worker tests; runtime coverage above configured thresholds. | Closed |
| D-013 | P1 | AT-048–056 | Metrics could be order-dependent, exceed valid rates, include unclassified captures, or omit failed attempts from coverage. | Cohort/version compatibility, deduplication, reviewed-fact precedence, bounded numerators, failed run-item coverage and drill-down records. | Analytics/metrics regression suites and production build. | Closed |
| D-014 | P1 | AT-024, AT-066 | Export could include managed evidence excerpts without resolving immutable/current source policy. | Citation-to-source-version lineage and fail-closed export redaction using both policy snapshots. | Restricted/unresolved/external citation export tests and hosted FK assertion. | Closed |
| D-015 | P1 | AT-061, AT-064 | Cross-instance rate limits and webhook event creation were not atomic; a table privilege was missing. | Atomic rate-window RPC, transactional webhook triggers, signed bounded delivery and explicit service privileges. | 13 API/webhook tests plus hosted privilege assertions. | Closed |
| D-016 | P1 | AT-061, AT-072, AT-075 | Health could report ready without DB/admin/worker configuration; unauthenticated API errors depended on admin configuration. | Readiness now proves PostgREST, Auth, strong worker/cron secrets, encryption and provider configuration; bearer parsing precedes admin creation. | Health/API tests and real HTTP boundary suite. | Closed |
| D-017 | P1 | AT-002, AT-010 | Auth redirect validation accepted backslash-normalized external targets. | Shared origin-checked relative-path parser rejects backslashes, controls and encoded variants. | Safe-next unit cases. | Closed |
| D-018 | P1 | AT-012, AT-019, AT-022 | External evidence fetches needed stronger SSRF and canonical redirect handling. | DNS-pinned HTTPS, full reserved IPv4/IPv6 blocking, per-hop validation, byte/time/redirect limits, final URL capture and tracking-parameter canonicalization. | Security and evidence-ingestion tests. | Closed |
| D-019 | P1 | AT-061, AT-073 | JSON size enforcement trusted `Content-Length`; weak worker secrets were accepted. | Stream-counted 64,000-byte cap and strong operational-secret validation used by auth and readiness. | 26 platform tests including chunked/understated bodies. | Closed |
| D-020 | P1 | AT-067–074 | Public E2E was not in CI, CSP retained unsafe inline directives, RTL/global-error/mobile sign-out coverage was incomplete. | CI installs Chromium/WebKit and runs Playwright; per-request nonce CSP, semantic error boundary, bidi primitives and mobile sign-out added. | 28-case discovery across four browser/device projects, static axe checks, HTTP tests and production build. | Closed |
| D-021 | P1 | AT-019–021 | File evidence, version append and archive were missing; archive enum mismatched the schema. | Private 1 MB MIME-limited bucket, safe parser/upload, append/archive RPCs, immutable history UI and canonical `archived` state. | 10 evidence lifecycle tests and hosted version/archive rollback probe. | Closed |
| D-022 | P1 | AT-056 | Usage UI compared project-only actuals with workspace-wide caps and omitted active reservations. | Workspace-wide actual usage plus active call/cost reservations are reconciled and shown separately. | Repository usage regression. | Closed |
| D-023 | P1 | AT-027 | Requiring a successful health check before the first manual run created a provider bootstrap deadlock. | Enabled configured providers may perform one bounded `unchecked` capture; degraded/unavailable/disabled providers remain blocked and schedules still require healthy status. | Repository unchecked-provider admission regression. | Closed |
| D-024 | P1 | AT-006, AT-019–021 | Workspace DB deletion did not by itself remove private Storage objects. | Deletion now atomically snapshots exact tenant-prefixed paths into a private service-only cleanup queue; immediate and worker cleanup use bounded leases, retries and terminal audit records. | Hosted owner-deletion/object-lease/completion assertions; worker and lifecycle tests. | Closed |
| D-025 | P1 | AT-017, AT-023, AT-025, AT-026 | Evidence freshness, duplicate/syndication, prompt-injection flags and conflict resolution were not user-visible. | Authority/freshness/policy are immutably snapshotted; claims, injection flags, independent-source grouping and deterministic conflict rationale are persisted and rendered. | 21 evidence tests plus hosted current/stale/unknown, tenant, immutability, archive and deletion-cascade assertions. | Closed |
| D-026 | P1 | AT-057, AT-058 | Action records did not require immutable evidence links or later-run follow-up. | Service-only action creation requires one immutable observed record, rationale, impact, effort and uncertainty; completion requires a later usable run and records no causation claim. | 6 focused tests plus hosted role, lineage, earlier-run rejection, completion and cascade assertions. | Closed |
| D-027 | P1 | AT-012, AT-019 | Project-domain diagnostics could save an unreachable or unsafe primary domain without actionable classification. | Setup performs the same DNS-pinned HTTPS checks used for evidence, canonicalizes safe redirects/IDNs, rejects unsafe or unavailable endpoints and preserves a warning for sparse but reachable pages. | 9 domain-diagnostics tests covering private DNS, redirects, status, size, timeout and canonicalization. | Closed |
| D-028 | P1 | AT-006, AT-057, AT-058 | The action follow-up FK used `ON DELETE RESTRICT`, allowing PostgreSQL cascade order to block project/workspace erasure. | Append-only FK replacement uses `ON DELETE CASCADE`; direct client mutation remains revoked and deferred lineage invariants still protect live actions. | Hosted project cascade and official `delete_workspace` rollback assertions. | Closed |
| D-029 | P2 | AT-073, AT-075 | New evidence/action actor foreign keys and cleanup-event lineage lacked covering indexes. | Added append-only covering indexes for all three relationships. | Full hosted schema assertion and database advisors. | Closed |
| D-030 | P1 | AT-042, AT-047 | Clause-level decision phrases could be attributed to every brand in a clause, while production never enabled common-word context safeguards. | Classifier v2.1 binds recommendation/rank signals to their grammatical brand target, lowers unbound claims into review, and enables context checks in the worker. | Adversarial classifier and worker regression suite. | Closed |
| D-031 | P1 | AT-049–056 | Rejected captures were omitted from recommendation denominators and current brand edits rewrote historical role/domain metrics; question-type/persona/competitor filtering and evidence support were incomplete. | Rejected eligible captures remain denominators, run-frozen brand versions drive history, all required cohort dimensions are filterable, and support requires a captured passage or immutable managed-source lineage. | Analytics/metrics unit and repository regressions. | Closed |
| D-032 | P1 | AT-036, AT-037, AT-059 | Schedule recurrence added UTC intervals, drifting across DST/month ends, accepted invalid zones, and exposed no controlled circuit reset. | IANA validation, local-wall-clock recurrence with day-31 clamping, and a service-only admin circuit-reset RPC/UI were added. | Hosted New York DST, February/March month-end, invalid-zone and reset rollback assertions. | Closed |
| D-033 | P1 | AT-032, AT-035, AT-039, AT-072 | Multi-call provider failures lost accumulated usage/raw details; worker timeout identity and expired-lease billing were ambiguous and could be double-accounted. | Provider error envelopes retain accumulated provenance, worker deadlines use a typed timeout, and lease recovery atomically writes one ambiguous attempt plus an idempotent usage event. | 37 focused runtime tests and hosted expired-lease accounting assertions. | Closed |
| D-034 | P1 | AT-006, AT-072 | Storage cleanup became permanently unclaimable after its eighth failure. | Terminal cycles now enter a visible 24-hour cooldown and the worker service revives them into another bounded, auditable retry cycle. | Lifecycle unit tests and hosted abandon/cooldown/revive/release assertion. | Closed |
| D-035 | P1 | AT-021, AT-026, AT-057, AT-058 | Claims could target archived source versions and trusted code could selectively delete one of several action-lineage links. | Active parent-source validation moved into the claim transaction; every live action link is immutable except an authorized parent/workspace cascade. | Hosted evidence and action-lineage rollback suites. | Closed |
| D-036 | P2 | AT-068–070, AT-072 | Authenticated loading used nested main landmarks; setup/decision contrast, bidi coverage, 320px header width, notices and operator zoom overflow had gaps. | Semantic status loading, AA colors, broader bidi isolation, compact mobile wordmark, live-region notices and bounded scrolling were added. | Static accessibility/DOM review, production build and four-project Playwright discovery. | Closed |
| D-037 | P1 | AT-035, AT-056, AT-061, AT-066 | Ambiguous provider billing looked like known zero cost, restricted managed citations retained export metadata, and quota conflicts collapsed to a generic API outage. | Usage exposes incomplete/ambiguous records, export redacts restricted/unresolved URL/title metadata, and authoritative quota failures return stable `quota_exceeded`. | Repository, export-policy and API regressions. | Closed |
| D-038 | P1 | AT-075 | A real authenticated Owner/Admin/Analyst/Viewer browser journey and live provider/email capture cannot run without test accounts, server secrets and provider credentials. | Code paths, CI projects and deterministic gates are ready; credentialed journey remains an external release gate. | Awaiting credentials listed in `.env.example` and installed Playwright browsers/deployment. | Blocked |

## Current gates

- Local deterministic gate: TypeScript and ESLint clean; 35 files / 227 tests pass; coverage is 83.6% statements, 74.37% branches, 93.41% functions and 89.76% lines; the Next production build generates all 34 application routes.
- Hosted database gate: all append-only migrations through timezone recurrence are applied; seven rollback-only schema/behavior suites pass; security advisor has only the intentional service-only rate-window info notice and performance advisor has only unused-index notices on the empty database.
- Browser gate: 48 public/security/credentialed cases are discovered across desktop/mobile Chromium and WebKit and enforced by CI; a local Playwright launch is blocked by this workspace's network/browser-binary policy. The credentialed cases are present but skip until the dedicated fixture variables are supplied, so authenticated/provider execution remains blocked as D-038.
- Live-provider and deployed authenticated smoke gates require the production server/provider/email secrets listed in `.env.example` and a linked deployment.
